# Copy to hosts.ini or pass this path to ansible-playbook with -i.
#
# The inventory name is the SSH destination: it may be a hostname, IP, or a
# `Host` alias from ~/.ssh/config (same string you use with `ssh <name>`).
[cognitify]
localhost ansible_connection=local
# Remote example (uncomment and adjust):
# base ansible_connection=ssh
