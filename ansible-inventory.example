# Example Ansible inventory file for Cognitify installation
# (c) 2026 Ramon Brooker <rbrooker@aeo3.io>
#
# Usage:
#   ansible-playbook -i ansible-inventory.example install.yml
#
# Or use a simple inventory:
#   ansible-playbook -i "host1,host2" install.yml

[servers]
# Example server entries
# server1.example.com
# server2.example.com ansible_user=admin

# Localhost example (for testing)
localhost ansible_connection=local
